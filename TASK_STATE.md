# TASK_STATE

## 目标

维护 Windows 版 Better Compact：在长任务中保留精简的项目状态，并在 compact 后恢复当前进度。

## 已确认边界

- 工作区为当前工作目录；状态文件位于唯一选定的项目根目录。
- Codex 负责注入 `AGENTS.md`；Better Compact 只恢复 TASK_STATE 提示、状态文件和 Recovery Card。
- 工作区运行文件和 Hook 独立于全局 Skill；安装 Hook 仍需用户批准。

## 已排除项

- 不记录完整对话、原始补丁、密钥或隐私；不把已完成步骤长期累积为日志。

## 当前状态

- 发行包已提供安装与迁移卸载入口；隔离安装及卸载测试覆盖现有流程。
- 旧工作区运行目录和 Hook 已卸载；当前机器的全局 Skill 也已移除，发行包源码仍在。
- Skill 与恢复提示已改为读取并精简重写当前状态；安装时会从发行包同步到全局 Skill。
- 安装入口现直接要求工作区路径；独立的 `G` 全局安装模式已移除，工作区安装仍附带全局 Skill。

## 下一步

在后续实际任务中观察状态是否保持精简。
